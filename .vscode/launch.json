{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}.out",
      "args": [],
      // 빌드 후 디버깅을 위해 task.json의 label 지정
      "preLaunchTask": "g++ build active file",
      "stdio": [null, null, null],
      "terminal": "integrated"
    }
  ]
}
